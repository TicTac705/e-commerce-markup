:focus вижу в очень многих местах - это лишнее,
корректнее вешать доп класс --active через js чтобы сделать
тоже самое, только при f5 еще и состояние будет сохраняться

часто применяешь display:flex и flex-direction: column зря.
если нужен отступ между блочными элементами (блоки и так всю строку занимают)
то можно сделать display: grid + gap: 8px например
